rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /telemetry/{docId} {
      allow read, write: if request.auth != null;
    }
    match /shadow_logs/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}
